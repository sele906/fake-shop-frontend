<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                공유 링크를 앱으로 받는다. 앱이 없는 사람에게는 그대로 웹페이지라
                도메인을 따로 살 필요가 없다 — 같은 주소가 둘을 겸한다.

                autoVerify는 설치 시 OS가 아래 주소를 직접 확인하게 한다.
                  https://fake-shop-frontend-ivory.vercel.app/.well-known/assetlinks.json
                여기 적힌 지문이 설치된 APK의 서명과 맞아야 "선택 창 없이" 앱이
                열린다. 어긋나면 조용히 브라우저로만 간다.

                공유하는 화면 둘만 받는다. host만 적으면 이 사이트의 모든 링크가
                앱으로 빨려 들어간다 — 검색 결과에서 눌러도 그렇게 된다.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="https"
                    android:host="fake-shop-frontend-ivory.vercel.app"
                    android:path="/receipt" />
                <data
                    android:scheme="https"
                    android:host="fake-shop-frontend-ivory.vercel.app"
                    android:pathPrefix="/product/" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
